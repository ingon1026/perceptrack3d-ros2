PyYAML==6.0.3
colcon-common-extensions==0.3.0   # Phase 9: .venv 안에서 colcon build (노드 shebang 을 venv python 으로)
filterpy==1.4.5   # notebooks_2nd 07: 칼만 필터 라이브러리 (실무 방식 비교용)
ipykernel==7.3.0
jupyter==1.1.1
matplotlib==3.11.1
nbconvert==7.17.1
nbformat==5.11.1
numpy==2.5.2
open3d==0.19.0
opencv-python==5.0.0.93
pandas==3.0.5
pybind11==3.1.0
pytest==9.1.1
scikit-learn==1.9.0
scipy==1.18.1
shapely==2.1.2   # notebooks_2nd 08: 회전 박스 IoU 비교용
torch==2.14.0+cpu
torchvision==0.29.0+cpu
tqdm==4.70.0
ultralytics==8.4.142
# 설치 방법 (README 참고). torch/torchvision 은 CPU 빌드를 먼저 설치한다:
#   pip install --isolated torch torchvision --index-url https://download.pytorch.org/whl/cpu
#   pip install --isolated --index-url https://pypi.org/simple -r requirements.txt
#   pip install --isolated --index-url https://pypi.org/simple -e .
